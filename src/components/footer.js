import React from 'react'

const Footer = () => {
    return (
        <footer>
            <div>TEI mit Gatsby/React { (new Date()).getFullYear() }</div>
        </footer>
    )
}

export default Footer